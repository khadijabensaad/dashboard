import 'package:flutter/material.dart';

class ReviewRoute extends StatelessWidget {
  const ReviewRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: const Center(child: Text("Review")));
  }
}

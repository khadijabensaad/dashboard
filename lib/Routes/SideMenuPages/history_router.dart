import 'package:flutter/material.dart';

class HistoryRoute extends StatelessWidget {
  const HistoryRoute({super.key});

  @override
  Widget build(BuildContext context) {
   return Container(child: const Center(child: Text("History")));
  }
}
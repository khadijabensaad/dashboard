import 'package:flutter/material.dart';

class MediaRoute extends StatelessWidget {
  const MediaRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: const Center(child: Text("Media")));
  }
}